using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace util
{
    public class StringUtil
    {
        /// <summary>
        /// Deze method interleaved in aString een character c op regelmatige intervals.
	    /// Bvb s = “blblbl”, c = ‘a’, interval = 2 wordt het resultaat: blablabla
        /// </summary>
        /// <param name="aString">de string waarin je wil interleaven</param>
        /// <param name="c">character om te interleaven</param>
        /// <param name="interval">om de hoeveel characters moet c geinterleaved worden in aString</param>
        /// <returns>aString met character c geinterleaved op interval indexen</returns>
        public static string interleave(string aString, char c, int interval)
        {
            throw new NotImplementedException("implement me");
        }
    }
}
